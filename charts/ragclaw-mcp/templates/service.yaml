apiVersion: v1
kind: Service
metadata:
  name: {{ include "ragclaw-mcp.fullname" . }}
  labels:
    {{- include "ragclaw-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ragclaw-mcp.selectorLabels" . | nindent 4 }}
